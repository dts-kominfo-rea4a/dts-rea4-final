import React from "react";

import LoginOrRegister from "../components/LoginOrRegister";

const Register = ({ loginOrRegister }) => {
  return <LoginOrRegister loginOrRegister="register" />;
};

export default Register;
